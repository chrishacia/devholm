# Issue 103 Legacy Path Inventory

This inventory maps legacy or transitional plugin architecture paths to canonical replacements, data risk, and phased removal gates.

## Inventory Table

| Legacy path                                                                                                                                | Current consumers                                                                                                                                                                                                                                                                                       | Canonical replacement                                                                                                                     | Data risk if removed early                                                                                               | Removal phase                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- |
| Direct registry import `bundledPlugins` from `@user/extensions/plugins/registry`                                                           | `src/core/lib/plugin-marketplace-admin.server.ts`, `src/core/lib/plugin-capability-sandbox.server.ts`, `src/core/lib/plugin-development-source-resolution.server.ts`, `src/core/plugins/first-party-marketplace-contract.ts`, `src/user/extensions/api/index.ts`, `src/user/extensions/admin/pages.tsx` | Use `getBundledPluginManifests()` and canonical resolver outputs from `plugin-canonical-resolver.server` + derived admin view projections | High: can desync runtime metadata, permissions, or surface ownership; can bypass canonical source-resolution diagnostics | A observe/reconcile, B canonical ownership, C removal |
| Transitional sync of plugin settings in `syncPluginDefinitions()`                                                                          | `src/core/db/plugins.ts` via `listPluginStates()` and `getPluginState()`                                                                                                                                                                                                                                | Lifecycle ledger (`devholm_plugins`) + durable lifecycle operations/events and migration checkpoints as source of state truth             | High: can recreate stale enabled flags and conflict with lifecycle ledger state intent                                   | A observe/reconcile, B canonical ownership            |
| Fallback-to-bundled state assumptions (`bundled: true`, `installed: false`, `lifecycleState: bundled`) when no ledger row exists           | `src/core/db/plugins.ts`                                                                                                                                                                                                                                                                                | Reconciliation-generated canonical classification + explicit migration state classes                                                      | High: can mask ambiguous/partial migration states and permit unsafe mutation                                             | A observe/reconcile                                   |
| Marketplace admin projection reads manifest metadata directly from `bundledPlugins`                                                        | `src/core/lib/plugin-marketplace-admin.server.ts`                                                                                                                                                                                                                                                       | Contract-driven metadata from canonical plugin source + lifecycle/state projections                                                       | Medium-high: drift between contract/ledger/runtime decisions                                                             | A observe/reconcile, B canonical ownership            |
| Startup reconciliation only at mutation/recovery entrypoints                                                                               | `src/core/lib/plugin-startup-reconciliation.server.ts`, `src/app/api/admin/plugins/route.ts`, `src/app/api/admin/plugins/recovery/reconcile/route.ts`                                                                                                                                                   | Split-mode startup APIs (`initialize...`, `ensure...ReadyForMutation`, state snapshot + dirty refresh)                                    | Medium: stale process cache can allow/deny wrongly after recovery changes                                                | A observe/reconcile                                   |
| Durable cutover reconciliation state absent                                                                                                | `src/core/lib/plugin-lifecycle-recovery-runner.server.ts`                                                                                                                                                                                                                                               | Durable cutover state + event tables (`devholm_plugin_cutover_reconciliation_states`, `...events`) with per-plugin phase evidence         | High: process restart loses in-memory-only cutover progress                                                              | A observe/reconcile (implemented)                     |
| Legacy/canonical topology handling lacked deterministic persisted outcomes                                                                 | `src/core/lib/plugin-cutover-legacy-reconciler.server.ts`                                                                                                                                                                                                                                               | Topology-aware reconciliation (`legacy-only`, `canonical-only`, `legacy-and-canonical`, `neither`) with fail-closed disagreement          | High: dual-state disagreement could otherwise drift into unsafe inferred ownership                                       | A observe/reconcile (implemented)                     |
| Explicit plugin-domain preservation proof missing for URL Shortener + Calendar                                                             | `src/core/lib/__tests__/plugin-cutover-url-shortener-preservation-postgres.integration.test.ts`, `src/core/lib/__tests__/plugin-cutover-calendar-preservation-postgres.integration.test.ts`, `src/user/extensions/plugins/gallery/tests/gallery-plugin-preservation-postgres.integration.test.ts`       | PostgreSQL preservation tests verifying row count, representative identity/value stability, and repeat-run cutover idempotency            | High: unproven preservation risk during cutover                                                                          | A observe/reconcile (implemented, expand in Phase B)  |
| Source resolution from raw `bundledPlugins` entries                                                                                        | `src/core/lib/plugin-development-source-resolution.server.ts`                                                                                                                                                                                                                                           | Canonical contract entries (`toCanonicalPluginConfigEntry`) + deterministic resolver result                                               | Medium: local override and canonical source consistency drift                                                            | A observe/reconcile, B canonical ownership            |
| Legacy installed-state assumptions in admin UI aggregation                                                                                 | `src/app/admin/plugins/page.tsx` via API payload from `listMarketplaceAdminPlugins`                                                                                                                                                                                                                     | Canonical axes summary from `deriveCanonicalMarketplaceLifecycleView()` + authority matrix                                                | Medium: contradictory operator UX under partial migration                                                                | A observe/reconcile                                   |
| Migration history uniqueness only at (`plugin_id`,`migration_id`,`direction`)                                                              | `src/core/db/plugin-lifecycle.ts` insert path + DB schema                                                                                                                                                                                                                                               | Add reconciliation checks for duplicate semantic executions and ambiguous execution identity; keep durable lock gates                     | High: replay risk under interrupted cutover/concurrency                                                                  | A observe/reconcile, B canonical ownership            |
| Runtime install bypass semantics via feature gate defaults (`runtimeInstallSupported: false`) and scaffold/readiness transitional statuses | `src/core/lib/plugin-marketplace-admin.server.ts`, marketplace install API routes                                                                                                                                                                                                                       | Explicit canonical readiness classes and trust/immutability checks; no implicit bypass path                                               | Medium: accidental assumption that non-install-ready paths are install-safe                                              | A observe/reconcile                                   |
| Legacy transitional term overlap (`installed` as historical, not full canonical active/deployed truth)                                     | docs + state aggregation                                                                                                                                                                                                                                                                                | Canonical state axes (`desired/resolution/build/deployment/runtime/trust/health/recovery`)                                                | Medium: operator confusion and unsafe manual actions                                                                     | A observe/reconcile, B canonical ownership            |

## Phase gates

### Phase A - Observe and reconcile

- Add deterministic reconciliation classifier over ledger/settings/migrations/checkpoints/source-resolution.
- Keep legacy reads where required, but route all writes through canonical lifecycle authority.
- Produce explicit classification outcomes and blocker reasons.

### Phase B - Canonical ownership

- Runtime/admin consumers read canonical reconciled state model first.
- Legacy path remains read-only compatibility input.
- Block creation of new legacy-only records.

### Phase C - Removal

## Non-removal rule

Do not remove a legacy path until each listed consumer is switched to a proven canonical replacement and reconciliation reports zero ambiguous/manual-intervention states for affected plugins.

## Incremental closure status (latest)

- Canonical write ownership:
  - `src/core/lib/plugin-lifecycle.server` no longer writes `plugin:*:enabled` during lifecycle mutations.
  - `src/core/db/plugins.ts` no longer seeds legacy enablement keys during definition sync.
  - Canonical runtime authority remains `devholm_plugins`.
- Runtime bypass decommission:
  - `src/core/lib/public-route-dispatcher.server` now uses `isPluginEnabledForRequest` instead of unconditional enablement.
  - Disabled plugin public routes are now fail-closed by canonical authority checks.
- Rollback stage foundation:
  - Added durable rollback stage checkpoint schema and APIs.
  - Recovery scan now records rollback checkpoints for rollback-required classifications and surfaces stage/availability metadata.
