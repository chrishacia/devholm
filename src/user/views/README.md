# Ejected core views live here.

#

# Use `pnpm devholm eject <view>` to copy a core view here for full customization.

# Available views: blog, blog-post, projects, resume, uses, search, contact, home, about, now

#

# After ejecting, register in devholm.config.ts:

# views: {

# blog: () => import('./src/user/views/blog/BlogView'),

# }

#

# Trade-off: ejected views no longer auto-update from upstream.

# Use `pnpm devholm status` to see which views you've ejected.
